# Codespaces-only additions, merged on top of ../docker-compose.yml.
# Paths are relative to the repo root (the first compose file's folder).
services:
  web:
    volumes:
      - .:/workspace:cached
      - ./.devcontainer/phpmyadmin-proxy.conf:/etc/apache2/conf-enabled/phpmyadmin-proxy.conf:ro
