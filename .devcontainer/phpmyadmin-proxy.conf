# Codespaces only: expose phpMyAdmin through the web container on port 8081,
# because Codespaces can only forward ports from the container VS Code runs in.
<IfModule !proxy_module>
  LoadModule proxy_module /usr/lib/apache2/modules/mod_proxy.so
</IfModule>
<IfModule !proxy_http_module>
  LoadModule proxy_http_module /usr/lib/apache2/modules/mod_proxy_http.so
</IfModule>
Listen 8081
<VirtualHost *:8081>
  ProxyPreserveHost On
  ProxyPass        / http://phpmyadmin:80/
  ProxyPassReverse / http://phpmyadmin:80/
</VirtualHost>
