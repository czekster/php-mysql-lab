# Copy this file to .env before running docker compose up.
# These are simple local-only defaults for coursework — fine to keep as-is
# for a sandbox on your own machine. Never reuse them anywhere real, and
# never commit your actual .env to git.

DB_ROOT_PASSWORD=rootpass123
DB_NAME=app_db
DB_USER=student
DB_PASSWORD=student123
